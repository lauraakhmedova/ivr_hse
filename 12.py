class article():
        def __init__(self, ID, subject, latitude, longtitute, added-by, url):
                self.ID = ID
                self.subject = subject
                self.latitude = latitude
                self.longtitute = longtitute
                self.added-by = added-by
                self.url = url
                
